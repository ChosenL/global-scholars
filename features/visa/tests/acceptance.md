# Visa feature acceptance

Executable repository checks live in `tests/visa-management.test.mjs`.
Database/RLS integration checks live in `supabase/tests/volume5_visa.sql`.

The database tests require the Volume 1–5 migrations to be applied to a
Supabase test instance.
