# Runtime manifests

Runtime manifest JSON files are ignored. Store only sanitized metadata and
checksums here locally; never store secrets. Production retention belongs in
immutable, access-controlled artifact storage.
