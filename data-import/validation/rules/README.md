# Validation rules

Executable structural, identity, relationship, and business-rule validation is
implemented in `scripts/lib/validation.mjs`. Extend rules with stable codes,
severity, entity identity, field path, and a secret-free message.
